# 📡 Tender Parsing

Веб-сервис для интеллектуального анализа тендеров с сайтов государственных закупок. Основная цель — автоматическое выявление потенциально проблемных закупок, в которых компании допустили ошибки или нарушили сроки исполнения контракта. Такой подход помогает юридическим компаниям находить клиентов, нуждающихся в правовой защите.

Проект разработан как полнофункциональное веб-приложение с авторизацией, интеграцией с внешними источниками, поддержкой ИИ-анализа и гибким управлением данными.

---

## 🌐 Доступ к проекту

Проект развернут в [Яндекс Облаке](https://cloud.yandex.ru) и доступен по адресу **tender-parsing.ru**

Вы можете войти в систему как пользователь или администратор (в зависимости от настроек доступа). Интерфейс оптимизирован для браузеров и включает в себя таблицы с тендерами, гиперссылки и административные функции.

---

## ⚒️ О реализации

Вся архитектура построена на принципах микросервисов с разделением фронтенда и бэкенда. Система полностью контейнеризирована с использованием Docker и может быть быстро развернута локально или в облаке.

* **Фронтенд**: написан на Next.js, реализует пользовательский интерфейс сервиса с использованием динамических секций страницы и модальных окон.
* **Бэкенд**: построен на FastAPI, отвечает за авторизацию, взаимодействие с БД, обработку запросов и реализацию парсера.
* **Парсинг**: модуль работает с сайтом zakupki.gov.ru, извлекая тендеры по заданным фильтрам.
* **ИИ-модуль**: анализирует прикрепленные документы с помощью LLM от Яндекса и выделяет ключевую информацию.
* **Хранение данных**: используется PostgreSQL, хранит все используемые бэкендом данные.
* **Развёртывание**: осуществляется с помощью Docker Compose. Также реализована возможность сборки и публикации образов на Docker Hub.

Проект написан с учётом масштабирования и возможности дальнейшего расширения.

---

## 📌 Техническое задание

Необходимо создать веб-сервис для анализа сайтов госзакупок (например, zakupki.gov.ru), где компании участвуют в тендерах. Если компания выиграла контракт, но не подписала его вовремя или нарушила условия, ей может грозить суд. Нужно сохранять информацию о таких тендерах и клиентах, чтобы предлагать им свои юридические услуги.

1. **Управление аккаунтом:** вход в систему с двумя ролями:

   * админ: может настраивать фильтры;
   * пользователь: может только просматривать итоговую таблицу.
2. **Интеграция с API для контента:**

   * получение данных с сайта [zakupki.gov.ru](https://zakupki.gov.ru);
   * запуск процедуры сканирования сайта на наличие новых тендеров, подходящих под критерии фильтра.
3. **Интерфейс пользователя:** наличие табличного отображения найденных тендеров с гиперссылкой на конкретный тендер.
4. **Интеграция с реляционной СУБД:** хранение данных пользователя, хранения распарсенных тендеров (PostgreSQL или аналоги).
5. **Развёртывание:** использование Docker и Docker Compose для работы сервиса.
6. **Углубленная интеграция с LLM**: анализ прикрепленных документов к тендеру с помощью LLM для поиска полезной информации о тендере.

---

## 📊 Технологический стек проекта

| Компонент          | Технологии                          | Ключевые функции                    |
|--------------------|-------------------------------------|--------------------------------------|
| **Frontend**       | Next.js 15, TypeScript, TailwindCSS | Интерфейс пользователя, визуализация данных |
| **Backend**        | Python 3.11, FastAPI, JWT           | REST API, бизнес-логика, безопасность |
| **Парсер**         | BeautifulSoup, Playwright           | Сбор данных с [zakupki.gov.ru](https://zakupki.gov.ru) |
| **Анализ документов** | PyPDF2, Yandex Vision OCR        | Обработка PDF и сканированных документов |
| **AI-модуль**      | YandexGPT 5                         | Юридический анализ контрактов         |
| **База данных**    | PostgreSQL, SQLAlchemy              | Хранение данных тендеров и пользователей |
| **Интеграции**     | Yandex Cloud API                    | Доступ к Vision OCR и GPT-сервисам    |
| **Контейнеризация**| Docker, Docker Compose              | Удобнобство запуска и хостинга        |
### Ключевые особенности:
- **Глубокая интеграция с Yandex Cloud**: Vision OCR для распознавания документов + YandexGPT для анализа
- **Гибкий пайплайн обработки**: автоматическое переключение между PyPDF2 и Vision OCR
- **Безопасность**: JWT-аутентификация с ролевой моделью (админ, юрист, пользователь)
- **Оптимизированный парсинг**: Playwright с режимом "анти-детект" для работы с [zakupki.gov.ru](https://zakupki.gov.ru)
- **Автоматизированный анализ**: YandexGPT 5 для выявления юридических рисков в контрактах

---

## 🏗 Структура проекта

```text
tender-parsing/
├── backend/                        #  Серверная логика (FastAPI)
│   ├── admin_requests/             #  Обработка админских запросов
│   │   └── routes.py               #  Роуты для админских запросов
│   ├── auth/                       #  Аутентификация и роли
│   │   ├── auth.py                 #  Логика входа/регистрации
│   │   ├── roles.py                #  Роли и доступы
│   │   ├── routes.py               #  Роуты аутентификации
│   │   └── security.py             #  Хеширование
│   ├── database/                   #  Работа с базой данных
│   │   ├── admin_requests.py       #  Фейковые модели и запросы админов для тестов
│   │   ├── database.py             #  Инициализация БД
│   │   ├── deps.py                 #  Зависимости FastAPI
│   │   ├── fake_users.py           #  Фейковые данные для тестов
│   │   └── models.py               #  SQLAlchemy модели
│   ├── llm/                        #  LLM и обработка текста
│   │   ├── analysis.py             #  Анализ данных
│   │   ├── fileLinkParser.py       #  Парсинг ссылок на файлы
│   │   └── ocr.py                  #  Распознавание текста (OCR)
│   ├── models/                     #  Pydantic-схемы
│   │   ├── admin_request.py        #  Схемы админских запросов
│   │   ├── parse.py                #  Схемы парсинга
│   │   └── user.py                 #  Схемы пользователей
│   ├── parser/                     #  Парсеры закупок
│   │   ├── routes.py               #  Роуты для парсера
│   │   └── zakupki_parser.py       #  Основной парсер закупок
│   ├── Dockerfile                  #  Контейнеризация бэкенда
│   ├── init_owner.py               #  Инициализация владельцев
│   ├── main.py                     #  Точка входа FastAPI
│   └── requirements.txt            #  Зависимости Python
│
├── frontend/                       #  Клиентская часть (Next.js)
│   ├── public/                     #  Публичные ресурсы (иконки, сведения для браузера)
│   ├── src/                        #  Исходный код приложения
│   │   ├── app/                    #  Страница, модуль и роутинг
│   │   ├── components/             #  Переиспользуемые компоненты
│   │   ├── libs/                   #  Утилиты и хелперы
│   │   ├── modal/                  #  Модальные окна
│   │   ├── providers/              #  Провайдеры контекста
│   │   └── types/                  #  Общие типы TypeScript
│   ├── Dockerfile                  #  Контейнеризация фронтенда
│   ├── eslint.config.mjs           #  Настройки линтинга
│   ├── next-env.d.ts               #  Типы окружения Next.js
│   ├── next.config.ts              #  Конфигурация Next.js
│   ├── package.json                #  Зависимости и скрипты
│   ├── package-lock.json           #  Лоч-файл npm
│   ├── postcss.config.mjs          #  Конфигурация PostCSS
│   ├── tailwind.config.ts          #  Конфигурация TailwindCSS
│   ├── tsconfig.json               #  Конфигурация TypeScript
│
├── .env                            #  Переменные окружения (конфиденциальные, установка дополнительно)
├── .gitignore                      #  Исключения Git
├── docker-compose.yml              #  Сборка и запуск всех сервисов
└── README.md                       #  Описание проекта

````

---

## 🚀 Установка и запуск проекта

Для запуска проекта **локально** вы можете выбрать один из трёх способов.

---

## 🧩 Вариант 1: Запуск проекта через установку проекта с GitHub

1. Перед началом убедитесь, что у вас установлен [Docker Desktop](https://www.docker.com/products/docker-desktop) для запуска контейнеров.

2. Перейдите на страницу [релизов](https://github.com/the-tender-team/tender-parsing/releases) проекта на GitHub.
 
3. Скачайте последний релиз — архив `.zip` для Windows или `.tar.gz` для Linux и разархивируйте его.

4. Перейдите в папку проекта:
```bash
cd tender-parsing
````
5. Создайте [файл `.env`](#-пример-env-файла) (в корне проекта).


6. Если вы хотите **вносить изменения в код**, потребуется установить зависимости и настроить окружение:

* **Linux/macOS:**
  ```bash
  # Установите Python (если ещё не установлен)
  sudo apt update && sudo apt install python3 python3-venv python3-pip

  # Установите Node.js и Next.js
  sudo apt install nodejs npm
  npm install -g create-next-app

  # Создайте и активируйте виртуальное окружение
  cd backend
  python -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt
  cd ../frontend
  npm install
  ```

* **Windows:**

  ```powershell
  # Установите Python: https://www.python.org/downloads/
  # Установите Node.js: https://nodejs.org/

  # Установите Next.js
  npm install -g create-next-app

  # Создайте и активируйте виртуальное окружение
  cd backend
  python -m venv venv
  .\venv\Scripts\activate
  pip install -r requirements.txt
  cd ../frontend
  npm install
  ```

7. **Соберите контейнеры и Запустите проект:**

```bash
cd ..
docker compose up --build
```

8. ⏱ **После запуска:**

   * Бэкенд будет доступен по адресу: `http://localhost:8000`
   * Фронтенд — по адресу: `http://localhost:3000`
   * Документация API (Swagger): `http://localhost:8000/docs`

---

### 🧩 Вариант 2: Клонирование репозитория и локальный запуск через Docker

1. **Перед началом убедитесь, что у вас установлены:**
> * [Git](https://git-scm.com/) — для клонирования репозитория
> * [Docker Desktop](https://www.docker.com/products/docker-desktop) — для запуска контейнеров


2. **Склонируйте репозиторий**:
```bash
git clone https://github.com/the-tender-team/tender-parsing.git
cd tender-parsing
````
Если вы хотите **вносить изменения в код**, потребуется установить зависимости и настроить окружение, посмотрите пункт 6 в первом способе.

3. Создайте [файл `.env`](#-пример-env-файла) (в корне проекта).

4. **Запустите проект:**

```bash
docker compose up --build
```

5. ⏱ **После запуска:**

   * Бэкенд будет доступен по адресу: `http://localhost:8000`
   * Фронтенд — по адресу: `http://localhost:3000`
   * Документация API (Swagger): `http://localhost:8000/docs`

---

### 📦 Вариант 2: Запуск только с Docker Hub (без исходного кода)

Если вы не хотите клонировать весь репозиторий, вы можете просто скачать готовые образы с Docker Hub.

> В этом случае вам нужно немного изменить `docker-compose.yml`, чтобы использовать **образы**, а не собирать их из локального кода.

1. **Создайте файл `docker-compose.yml` следующего вида:**

```yaml
version: '3.8'

services:
  db:
    image: postgres:16
    container_name: zakupki_postgres
    restart: unless-stopped
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    image: shershaveliy/zakupki-backend:latest
    container_name: zakupki_backend
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    image: shershaveliy/zakupki-frontend:latest
    container_name: zakupki_frontend
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  pgdata:
```

2. Создайте [файл `.env`](#-пример-env-файла) (в корне проекта).

3. **Запустите сервисы:**

```bash
docker-compose up
```

4. ✅ Готово! Приложение запущено:

   * Бэкенд: [http://localhost:8000](http://localhost:8000)
   * Фронтенд: [http://localhost:3000](http://localhost:3000)

---

## 📂 Пример `.env` файла

Файл `.env` содержит конфиденциальные данные и **не загружен в репозиторий**. Создайте его вручную в корне проекта и впишите необходимые пароли и токены:

```env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=
DATABASE_URL=
YANDEX_OAUTH_TOKEN=
YANDEX_OCR_TOKEN=
YANDEX_LLM_FOLD_ID=
OWNER_USERNAME=
OWNER_PASSWORD=
SECRET_KEY=

```

---

### 💡 Полезные команды

| Команда                  | Описание                             |
| ------------------------ | ------------------------------------ |
| `docker-compose down`    | Остановить и удалить контейнеры      |
| `docker-compose build`   | Пересобрать контейнеры вручную       |
| `docker-compose logs -f` | Просмотр логов в реальном времени    |
| `docker system prune -a` | Очистить неиспользуемые образы и кэш |

---

### 🛠 Поддержка

Если у вас возникают ошибки или сложности — создайте [issue](https://github.com/your-org/tender-parsing/issues) в репозитории, и мы поможем вам разобраться 💬

---
